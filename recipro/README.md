# Recipro Evolution

Aplicação web para análise de reciprocidade emocional em conversas WhatsApp exportadas em `.txt`.

## Rodar localmente

```bash
pip install -r requirements.txt
uvicorn main:app --reload
```

> Execute os comandos a partir da pasta `recipro/`.
